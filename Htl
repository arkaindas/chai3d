const jobName =
  parentList.find((p) => p.jobNameMasterId === formData.jobNameMasterId)
    ?.jobName || "";

const rows = Array.isArray(res.data)
  ? res.data.map((row) => ({ ...row, jobName }))
  : [];

setList(rows);
