marks: isFMGEQual
    ? (d.fmgeDetails?.scoreObtained ? Number(d.fmgeDetails.scoreObtained) : null)
    : (d.marks !== "" && d.marks !== null && d.marks !== undefined
        ? Number(d.marks)
        : null),
